import React from 'react'
import Spinner from 'react-spinkit'

import './styles.css'

const FenSpinner = ({ color }) => (
  <Spinner id="fd-spinner" name="pacman" color={color}/>
)

export default FenSpinner
